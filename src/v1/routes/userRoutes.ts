import { Router } from "express";

const router = Router()
router.get('/users', () => { })
router.post('/users', () => { })
router.get('/:id', () => { })
router.put('/:id', () => { })
router.delete('/:id', () => { })

export { router }